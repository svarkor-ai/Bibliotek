# MC#2317 requirements for svarkor-ai/bibliotek -- exact pins (vm106 build gate: pkg==version only,
# no ranges, no extras, no inline comments; <=20 pins, <=4096 bytes). Derived from the ranged
# pyproject deps + the actual imports (fastapi/jinja2/jose/bcrypt/httpx/sqlalchemy).
fastapi==0.115.0
uvicorn==0.34.0
sqlalchemy==2.0.36
jinja2==3.1.4
python-jose==3.3.0
cryptography==43.0.3
bcrypt==4.2.0
httpx==0.27.2
python-multipart==0.0.12
pydantic==2.9.2
